# Your domain (must have an A record pointing to your VPS IP)
DOMAIN=scripper.yourdomain.com

# Server
PORT=3001
NODE_ENV=production

# Authentication — CHANGE THESE BEFORE DEPLOYING
# Generate JWT_SECRET with: openssl rand -hex 32
JWT_SECRET=change-me-to-a-random-string-at-least-32-characters-long
AUTH_PASSWORD=change-me-to-a-strong-password

# CORS — must match your domain with https://
CORS_ORIGIN=https://scripper.yourdomain.com

# Frontend (leave empty — nginx proxies API requests)
VITE_API_URL=
